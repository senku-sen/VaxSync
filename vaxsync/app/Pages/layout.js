export const metadata = {
  title: 'VaxSync - Vaccine Inventory Management',
  description: 'Real-time vaccine inventory management system',
};

export default function PagesLayout({ children }) {
  return children;
}
