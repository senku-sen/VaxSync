export const metadata = {
  title: "Reports & Analytics - VaxSync",
  description: "Vaccine usage and vaccination reports",
};

export default function ReportsLayout({ children }) {
  return children;
}
